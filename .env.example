# API Configuration
# Leave empty for SSR API (same domain: /api)
# Or set external API URL for separate backend server
# NUXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# NUXT_PUBLIC_API_BASE_URL=https://api.yourapp.com/v1
NUXT_PUBLIC_API_BASE_URL=

# Database Configuration (if needed)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# JWT Configuration (if needed)
JWT_SECRET=your-jwt-secret-key
JWT_EXPIRES_IN=7d

# Email Configuration (if needed)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-password

# External Services (if needed)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Storage Configuration
# AWS S3 Configuration (optional - if not set, files will be stored locally)
# To enable S3 uploads, configure all AWS variables below:
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=ap-southeast-1
AWS_S3_BUCKET=your-s3-bucket-name
# AWS_S3_URL=https://your-cloudfront-domain.com  # Optional: CloudFront or custom domain URL

# App Configuration
APP_NAME="Digital Agency"
APP_URL=http://localhost:3000
APP_ENV=development